/**
 * Contains file types for `Tools.readFile(path, type)`.
 */
class FileType {
    static text() {
        return 0;
    }
    static bytes() {
        return 1;
    }
    static json() {
        return 2;
    }
}